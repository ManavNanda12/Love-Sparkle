## Packages
framer-motion | Essential for complex animations (running button, floating hearts)
canvas-confetti | For the heart-shaped fireworks celebration
@types/canvas-confetti | TypeScript definitions for confetti

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  handwriting: ["var(--font-handwriting)"],
}
